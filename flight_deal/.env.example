
# Required
TEQUILA_API_KEY=replace_with_your_tequila_api_key
ORIGIN_IATA=LON
CURRENCY=GBP

# Search window (defaults used if omitted)
DATE_FROM_DAYS=1
DATE_TO_DAYS=180
NIGHTS_MIN=3
NIGHTS_MAX=14

# Max stopovers (0 for direct only, 1 to allow one stop, etc.)
MAX_STOPOVERS=1

# Optional: SMTP Email
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
FROM_EMAIL=
TO_EMAIL=

# Optional: Twilio SMS
TWILIO_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM=
TWILIO_TO=
